# 笔画·增强 (stroke_zh) 方案设计 v1.1

> 本文档记录方案的完整设计：目标、历史问题、调研结论（Google Gemini）、最终方案（含对调研建议的修改与理由）、验证结果。
> 最后更新：2026-08-08（v1.1.0 单字/词组彻底分流）

---

## 1. 设计目标与核心体验

- **输入方式**：五笔画（横竖撇捺折），键位 `h/s/p/n/z`，兼容 `d→n`、`t→h`、Mac 排位 `j/k/l/u/i`
- **核心 UX（用户实测定义）**：**每个字打 3 笔 → 常用字第一页上屏**
- 词组可打（每字前 4 笔连续打），但**绝不干扰单字输入**
- 纯离线，RIME 标准（元书输入法导入），iOS 字体全支持无乱码

## 2. v1.0 的问题（本版修订动机）

v1.0 = 单字 7953 + 雾凇词组 62 万（624,381 条词条 = 每词全码 + 4 笔简码各一条）。

实测问题（用户反馈 + 离线模拟证实）：

| 现象 | 数据 |
|---|---|
| 打 3 笔前缀时词组全参与竞争 | 每个 3 笔前缀命中 2,000~42,000 条词组 |
| 常用单字被挤出第一页 | 子 #35、天 #57、真 #179、太 #61（3 笔时） |
| 用户具体反馈 | 「我/这」打不出来 |
| 同词重复占位 | 全码 + 简码两条都命中前缀 → 同一词占 2 个候选位 |

**根因**：`enable_completion: true` 前缀联想下，3 笔输入会命中所有「编码以此 3 笔开头」的词组。词组量级过大时，单字（编码全码，最长 13 笔）在短前缀阶段毫无竞争力。

## 3. 调研：Google Gemini（2026-08-08）

调研问题：笔画输入法如何设计才高效（主流方案排序策略 / 简码体系 / 词组编码 / RIME 做法 / 权重策略）。

结论要点：

1. **单字与词组彻底分流**（iOS / 搜狗模式）：前 1~3 笔为「纯字禁飞区」，候选只展示单字；词组必须达到编码门槛（且精确匹配）才参选
2. **简码体系**：一/二/三级简码 = 取该字真实笔画前 1/2/3 笔 + 赋予高权重（5/25/125 槽位锁高频字）
3. **词组取码**：定长截取（二字词 = 首字前 2 笔 + 次字前 2 笔，总 4 笔；三字词 2+2+2；四字及以上前三末一）
4. **RIME 多 translator 分字典**：单字 translator `enable_completion: true` + 词组 translator `enable_completion: false`
5. **权重策略**：静态字频（国家语委频度表）+ 用户词典动态调频（`user_dict`）
6. Lua 过滤器兜底（输入 ≤3 笔时强制单字优先）

## 4. 最终方案 v1.1（采纳 + 修改）

### 4.1 与调研建议的差异（我们的修改与理由）

| 项 | Gemini 建议 | 我们的修改 | 理由 |
|---|---|---|---|
| 词组编码长度 | 每字前 2 笔（二字词 = 4 笔码） | **每字前 4 笔**（二字词 = 8 笔码） | 62 万词下 4 笔码仅 5⁴=625 个槽位 → 每码约 1,000 词灾难性撞码；8 笔码 5⁸≈39 万槽位 → 均码 <1 词，精确匹配可行 |
| 词组出现门槛 | ≥4 笔可参选 | **打满编码才出现**（二字词 = 8 笔） | 8 笔是词组最短码；配合 `enable_completion: false` 自然实现「禁飞区」 |
| 一/二/三级简码表 | 显式分配 5/25/125 槽位 | **不建表** | 前缀联想 + 字频排序天然等价（打 1 笔 = 最高频起笔字；打 3 笔 = 同前缀最高频字），零维护 |
| 词组全码 | 未提及 | **删除**（只留简码） | 全码太长无人打，且前缀阶段与简码重复占位 |
| Lua 过滤器 | 兜底保障 | **不用** | 双 translator 分流已彻底解决；元书对 Lua 支持不确定，保持简单可靠 |
| 用户词典词组 | — | 学到的词组走主 translator，**仍可前缀联想** | 自动造词 encoder 公式与词组编码同规则（每字前 4 笔），见 4.4 |

### 4.2 数据层（构建产物）

| 文件 | 内容 | 权重 |
|---|---|---|
| `stroke_zh_base.dict.yaml` | 单字 **7,953**（GB2312 6763 + 通用规范表 8105 基本区增量 1190，过滤 273 个扩展区字防乱码） | jieba 单字频次；55 个高频无搭配字 ×100 |
| `stroke_zh_phrase.dict.yaml` | 词组 **314,153**（雾凇 54 万词，词频 ≥50 过滤，GB2312 字过滤） | 雾凇词频 ×10 |
| `stroke_zh.dict.yaml` | 主表：**仅 import base** + encoder 规则 | — |

词组条目从 624,381 → 314,153（去全码，文件 20.3MB → 8.7MB，zip 7.1MB → 4.6MB）。

### 4.3 方案层（schema：双 translator 分流）

```yaml
engine:
  translators:
    - predict_translator
    - reverse_lookup_translator
    - punct_translator
    - table_translator                # 单字: enable_completion: true
    - table_translator@stroke_phrase  # 词组: enable_completion: false
```

- **主 translator**（`stroke_zh`，仅单字表）：前缀联想 + 用户词典 + 自动造词（encoder）
- **词组 translator**（`stroke_zh_phrase`）：精确匹配 + 词组用户词典（常用词组自动前置），`enable_encoder: false`
- **效果**：打 1~7 笔候选 100% 单字；打满 8/12/16/20/24 笔（2/3/4/5/6 字词）精确出词组

### 4.4 编码规则

| 对象 | 编码 |
|---|---|
| 单字 | 真实笔画全码（GB/T 25741 大陆笔顺，hspnz） |
| 词组 | 每字前 4 笔拼接（不足 4 笔打全部），如 我们 = `phshpsns`、中国 = `szhsszhh` |
| 自动造词 | encoder 公式 `AaAbAcAdBaBbBcBd...`（每字前 4 笔，与词组规则一致）→ 学到的词进用户词典，可前缀联想 |
| 上屏预测 | predict.db（雾凇二元组），`max_candidates: 9` / `max_iterations: 3` |

### 4.5 权重策略

1. **静态字频**：单字 = jieba 单字频次；词组 = 雾凇词频 ×10（量级平衡）
2. **高频字加成**：55 个常用但少组词的字（的/有/个/是/在/了/我/你/他…）×100，打前几笔即靠前
3. **动态调频**：主 translator 与词组 translator 都开 `enable_user_dict`——用户选过的字/词自动前置，越用越顺手

## 5. 验证结果（离线模拟，2026-08-08）

### 5.1 单字 3 笔第一页命中（47/47）

打 3 笔（编码前 3 笔）时，47 个常用字**全部第一页（≤9）**，代表字：

| 字 | 编码 | 3 笔前缀 | 排名 | | 字 | 编码 | 3 笔前缀 | 排名 |
|---|---|---|---|---|---|---|---|---|
| 我 | phshzpn | phs | #2 | | 子 | zsh | zsh | **#1**（原 #35） |
| 你 | pspzspn | psp | #1 | | 天 | hhpn | hhp | **#1**（原 #57） |
| 这 | nhpnnzn | nhp | #1 | | 真 | hsszhhhpn | hss | **#1**（原 #179） |
| 的 | pszhhpzn | psz | #2 | | 太 | hpnn | hpn | #2（原 #61） |
| 在 | hpshsh | hps | #1 | | 最 | szhhhsshhn | szh | #7 |
| 是 | szhhhshpn | szh | #1 | | 里 | szhhshh | szh | #6 |

「我」排 #2（「和」#1）为字频排序的正常结果；用户词典学习后会自动前置。

### 5.2 词组 8 笔精确匹配

| 词 | 8 笔码 | 同码数 | 排名 |
|---|---|---|---|
| 我们 | phshpsns | 1 | #1 |
| 他们 | pszspsns | 1 | #1 |
| 中国 | szhsszhh | 88 | #1 |
| 人民 | pnzhzh | 1 | #1 |
| 可以 | hszhznpn | 4 | #1 |
| 什么 | pshspzn | 2 | #1 |

### 5.3 静态校验

`validate.py` 全过：dict 头部闭合、314,153 条编码全部合法（仅 hspnz）、schema/皮肤 YAML 正常。

## 6. 构建与部署

- **构建**：`build.py`（iSH 本地慢 → 打包传远程 Mac `/opt/homebrew/bin/python3.14`，1.8s 完成 → 产物拿回；注意 Mac 默认 python3.9.6 不支持 `list[str] | None`，需 3.10+；Mac 上 build/ dist/ 目录需先建）
- **打包**：`build.py` 只生成 yaml，`predict.db` 需手动加回 zip
- **导入**：元书输入法删除旧 stroke_zh 方案 → 导入新 zip（皮肤 cskin 不变）

## 7. 版本记录

| 版本 | 内容 |
|---|---|
| 1.0.0 | 单字 7953 + 雾凇词组（全码 + 4 笔简码并存，前缀联想） |
| **1.1.0** | **单字/词组彻底分流**：词组只留 4 笔简码；双 translator（单字前缀联想 + 词组精确匹配）；常用单字 3 笔即上屏 |

---

*调研来源：Google Gemini 网页版（2026-08-08），结论摘要见第 3 节。*
